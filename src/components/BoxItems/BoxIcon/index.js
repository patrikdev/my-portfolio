import React from "react";
import * as S from "./styles";

export const LogoCourse = ({ icon }) => {
  return (
    <S.Container>
      <S.Image icon={icon} />
    </S.Container>
  );
};

export default LogoCourse;
